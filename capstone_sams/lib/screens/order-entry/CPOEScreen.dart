import 'package:flutter/material.dart';

class CPOEScreen extends StatelessWidget {
  const CPOEScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blueAccent,
      child: Text('CPOEEE'),
    );
  }
}
